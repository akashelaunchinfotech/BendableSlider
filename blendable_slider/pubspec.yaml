name: bendable_slider
description: "A bendable curved slider with gradient progress and custom thumb."
version: 0.0.1
homepage: https://github.com/akashelaunchinfotech/BendableSlider/tree/master/blendable_slider

environment:
  sdk: ^3.8.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true